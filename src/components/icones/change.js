import React from 'react';
import { Icon } from '@material-ui/core';
import ChangeCircleIcon from '@mui/icons-material/ChangeCircle';

const change = () => (
  <Icon>
    <ChangeCircleIcon style={{ color: '#fff' }} />
  </Icon>
);

export default change;
