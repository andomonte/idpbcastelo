import React from 'react';
import { Icon } from '@material-ui/core';

function IconeInstalar() {
  return (
    <Icon>
      <img src="/images/install.png" height={25} width={25} alt="install" />
    </Icon>
  );
}

export default IconeInstalar;
